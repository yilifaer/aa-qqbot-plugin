# aa-qqbot v1 设计文档（AllianceAuth 端）

> 状态：**草案，等待所有者确认**。确认之前不写代码。
> 所有决定的来源见 [`DECISIONS.md`](DECISIONS.md)，文中用「决定 #N」引用。
> 本文件不包含任何群号、机器人 QQ、服务器地址或密钥，仓库公开也不会泄露。

---

## 1. 这个插件做什么

联盟用 QQ 群沟通，目标是 **只有 AA 认可的人才能留在对应的 QQ 群里**。

整个系统分两半：

| 部分 | 在哪里 | 负责什么 |
|---|---|---|
| **AA 插件**（本仓库） | 装在联盟的 AllianceAuth 上 | 成员绑定 QQ；QQ 管理员在前台管理群和成员；回答机器人的问题「这个 QQ 能不能进这个群、群名片应该叫什么」 |
| **Koishi 插件**（另一个仓库） | 装在 QQ 机器人上 | 审批入群申请、定时巡检、踢人（默认只报告不踢）、改群名片 |

**通信只有一个方向：机器人主动问 AA。** AA 在公网，机器人在内网，AA 连不到机器人。所以 AA 这边只负责「回答问题」和「记下发生了什么变化」，由机器人定时来取。

---

## 2. 谁能做什么（权限）

插件定义两个权限，都在 admin 里挂到**状态（State）或组（Group）**上（决定 #4、#7）：

| 权限 | 中文名 | 起步挂给 | 能做什么 |
|---|---|---|---|
| `qqbot.basic_access` | QQ 绑定 - 成员 | Member 状态 | 在服务页看到「QQ 绑定」；绑定、换绑、解绑**自己的** QQ；看到自己有资格进的群 |
| `qqbot.manage` | QQ 绑定 - 管理员 | 由你指定的组（例如「QQ 管理」组） | 进入前台管理页（见 §4.3） |

补充说明：

- **超级管理员**：AA 的规则是超级管理员什么权限都有，所以一定能看到这个服务。但**判断能不能进群时，不会因为是超级管理员就自动放行**，而是和普通人一样看是否真的被授予了 `basic_access`。
- **被停用的账号**（`is_active=False`）一律不合格。AA 自己的状态计算不看这一项，所以插件要单独检查。
- **没有主角色**的账号一律不合格。

---

## 3. 群的配置（QQ 管理员在前台设置）

每个 QQ 群是一条记录（决定 #8、#9、#10）：

| 字段 | 说明 | 例子 |
|---|---|---|
| 群名称 | 显示给成员看 | 联盟聊天群 |
| 群号 | 5–11 位数字 | 123456789 |
| 类型 | **固定群** / **身份组小群** | 固定群 |
| 需要的 AA 组 | 只有身份组小群需要填，**满足其中任意一个即可** | Cap、FC |
| 说明 | 显示在群号下面的一句话 | 全联盟日常聊天 |
| 排序 | 页面上的显示顺序 | 10 |
| 启用 | 关闭后成员看不到，机器人也不再管这个群 | ✅ |

**入群资格 = 基础条件 + 额外条件：**

```
基础条件（所有群都要）：账号未停用 且 有主角色 且 被授予 basic_access（不算超级管理员的自动放行）
额外条件（只有身份组小群）：属于「需要的 AA 组」中的任意一个
```

「真踢人」的开关**不在 AA 里**，只能在机器人那边打开（决定 #11）。AA 前台只决定「哪些群归机器人管、谁有资格」。

---

## 4. 页面

### 4.1 服务页中的卡片

AA 5 的「服务」页里，每个服务是一张卡片（和 Discord 那张并排）。我们的卡片显示：

```
┌──────────────────────┐
│       QQ 绑定         │
│   [已启用] / [未启用]  │  ← 另有 [待验证]（蓝）、[冲突] / [已被占用]（红）
│      12****78        │  ← 已绑定时显示打码的 QQ；待验证或冲突时显示提示
│  ──────────────────  │
│      [ 管理 ]         │  ← 跳转到下面的完整页面
└──────────────────────┘
```

表单和入群须知放在完整页面里，卡片只做入口。菜单栏同时加一个「QQ 绑定」入口，只对有 `basic_access`（或 `qqbot.manage`）的人显示；
只有 `qqbot.manage`、没有 `basic_access` 的管理员点它会直接进入管理页。

### 4.2 「我的 QQ」页（成员）

**绑定前：**

```
群名片： [IGC] Kaela Voss - [ 昵称输入框 ]
QQ 号：  [ QQ号输入框 ]
         [ 提交 ]

入群须知（QQ 管理员可以在前台修改）
  加入联盟 QQ 群即表示你同意遵守群规，入群后请认真阅读群公告。
  · 禁止传播涉黄、涉赌、涉毒的内容，包括讨论、链接、图片和视频；
  · 禁止人身攻击、种族歧视、造谣诽谤，以及低俗、暴力或其他负面内容；
  · 禁止讨论政治话题，避免触及敏感内容。
  让我们一起维护健康、友好的交流环境。
```

- `[IGC]` 和 `Kaela Voss` 自动取自主角色的军团简称和角色名，成员不能改。
- 昵称限制：最长 12 个字；只允许中文、英文字母、数字、空格和 `_ - . · ( )`。
- 提交后按 §5 分成两种情况：**老成员直接生效**，**新成员显示验证码**。

**绑定后：**

```
✅ 已绑定 QQ：12****78（已验证 / 老成员免验证）

我的群名片预览：[IGC] Kaela Voss - 凯拉

你可以加入的群：
  固定群
    联盟聊天群   123456789   全联盟日常聊天
    Ping 群      987654321   集结通知
  身份组小群
    旗舰群       111222333   Cap 组专用

[修改昵称]   [换绑 QQ]   [解除绑定]   （解除需要二次确认）
```

- 只列出**当前有资格**的群。
- 状态为「冲突」时只显示提示「这个 QQ 同时被其他账号认领，请联系 QQ 管理员」，不显示群号。

### 4.3 前台管理页（QQ 管理员，不需要进 Django 后台）

| 标签页 | 能做什么 |
|---|---|
| **QQ 群** | 新增、编辑、停用群；设置类型和需要的 AA 组（§3） |
| **已绑定成员** | 按角色名、QQ、军团搜索；看到每个人的绑定状态、资格、群名片；可以**修改群名片**（覆盖自动生成的名片）、**确认绑定**、**强制解绑** |
| **待处理** | 冲突的 QQ（两个账号认领同一个号）、群里有但没有绑定的 QQ（来自机器人最近一次巡检），逐条处理 |
| **设置** | 入群须知文字、群名片格式、验证码有效期 |
| **操作记录** | 谁在什么时候对哪个 QQ 做了什么（绑定、解绑、换绑、确认、改名片、改群配置） |

只有一样东西**不放在前台**：机器人和 AA 之间的**通信密钥**。它由 IT 写在 `local.py` 里，这样就算管理员账号被盗，也拿不到这把钥匙。

---

## 5. 绑定流程

### 5.1 三种绑定状态

| 状态 | 怎么来的 | 能不能进群 | 独占这个 QQ 吗 |
|---|---|---|---|
| **已验证** | 用了验证码，或 QQ 管理员点了「确认」 | 能 | **独占**：别人不能再认领 |
| **老成员免验证** | 填写时这个 QQ 此刻就在受管群里 | 能 | 不独占：别人也填了同一个号时变成冲突 |
| **待验证** | 填写时这个 QQ 不在任何受管群里 | **不能**，等验证码被使用 | 不独占 |

### 5.2 老成员（已经在群里的人）（决定 #13、#14）

1. 在「我的 QQ」页填写昵称和 QQ 号，点提交。
2. AA 查询机器人最近一次巡检交上来的群成员名单：**这个 QQ 此刻在某个受管群里** → 直接生效，状态为「老成员免验证」。
3. 完成。整个过程不需要碰 QQ。

「此刻在群里」的定义：出现在该群**最近一次成功巡检**的成员名单里，并且那次巡检发生在 7 天以内。如果机器人超过 7 天没交名单，就退回到「待验证」流程，防止用过期名单放行。

### 5.3 新成员（还不在群里的人）

1. 在「我的 QQ」页填写昵称和 QQ 号，点提交。
2. AA 发现这个 QQ 不在任何受管群里，状态为「待验证」，页面显示一个**验证码**（例如 `QQ-7K3F9P`，10 分钟有效，只能用一次）和操作指引：
   > 请在 10 分钟内申请加入下面的群，在「验证信息」里填写：QQ-7K3F9P
3. 机器人收到入群申请，把「申请人 QQ + 验证码」发给 AA。
4. AA 检查：验证码有效、没用过、属于填写的这个 QQ，而且申请人确实是这个 QQ（机器人给出的申请人 QQ 由 QQ 服务器提供，无法伪造）→ 状态变为「已验证」→ 告诉机器人「放行」。
5. 验证码过期可以在页面上重新生成。为了安全，数据库里不保存验证码原文，所以验证码只在生成它的那个浏览器里能再次看到；换了电脑或手机看不到时，点「重新生成」即可（旧码自动作废）。

接口也支持「私聊机器人发送验证码」，机器人那边以后要做随时可以接上，AA 这边不用再改。

### 5.4 冲突（同一个 QQ 被两个账号认领）

- 两个账号都是「老成员免验证」或「待验证」→ 两边都标记为**冲突**。机器人对冲突的 QQ **永远不踢、不拒绝**，只报告；QQ 管理员在「待处理」里决定归谁。
- 其中一方用验证码完成验证，或被管理员确认 → 这一方胜出，另一方的认领作废，并写进操作记录。
  注意：号主还在群里时，这个 QQ 在最新名单里，填写它只会得到「老成员免验证」，拿不到验证码；
  所以这种冲突实际上只能由 QQ 管理员在「待处理」里确认或强制解绑来解决。
- 某个 QQ 已经「已验证」→ 其他人**不能再填写**这个号，页面提示「该 QQ 已被绑定，如有疑问请联系 QQ 管理员」。

### 5.5 换绑、解绑、账号删除

- **换绑**：新 QQ 按 5.2 或 5.3 走完流程后，才替换旧 QQ（原子操作）。同时产生一条「旧 QQ 失去资格」事件，让机器人复查旧号。换绑有冷却期（默认 24 小时），防止频繁换号。
  先解绑再填新号**不会**绕过冷却（冷却从上次换号算起；重新绑定刚解绑的同一个号不受限制）；
  老成员免验证的绑定另有频率限制（每小时最多 5 次）。
- **解绑**：成员自己可以解绑（二次确认），QQ 管理员可以强制解绑。都会产生「失去资格」事件。
- **AA 账号被删除**：删除前先记下 QQ 并产生事件，再删除绑定，不留孤儿记录。

---

## 6. 群名片（决定 #12）

- 默认格式：`[{军团简称}] {主角色名} - {昵称}`，例如 `[IGC] Kaela Voss - 凯拉`。QQ 管理员可以在「设置」里改格式。
- QQ 管理员可以给某个人**单独设置名片**，覆盖默认格式。
- 换军团、换主角色、改昵称后，名片会**自动**跟着变。
- AA 只负责**算出**每个人应该叫什么，把名片放进给机器人的回答里；**真正修改群名片由机器人执行**。
- QQ 群名片有长度上限（**待实测**，暂按 60 字节处理）。超长时页面上会提示，并自动截断角色名部分。

---

## 7. 机器人怎么知道「谁失去了资格」

两条渠道同时存在，互为备份：

1. **定时巡检（主渠道）**：机器人每隔一段时间（建议 6 小时，因为 AA 自己也是每 6 小时刷新一次角色所属）把整个群的成员名单**一次性**交给 AA，AA 对名单里的每个 QQ 给出判定和应有的群名片。顺便更新 §5.2 用到的「谁在群里」名单。
2. **变化事件（快速通道）**：发生停用、降级、退出组、解绑、换绑、名片变化时，AA 在数据库里记一条事件。机器人每分钟来取一次新事件，几十秒内就能反应。

另外有一个**每日对账任务**（Celery 定时任务，由 IT 在 `local.py` 里注册）：重新计算所有绑定，补上信号可能漏掉的事件，例如批量修改。

---

## 8. 给机器人的接口（概要）

完整的接口契约会单独写成 `API.md`，包括字段、错误码，以及可以直接比对的**签名测试样例**，Koishi 仓库照着它实现。这里只列概要。

所有接口：

- 都是 `POST`，都返回 JSON，**AA 内部出错也返回 JSON，不会返回网页**
- 都要签名：`HMAC-SHA256(密钥, 方法 + 换行 + 路径 + 换行 + 时间戳 + 换行 + 随机数 + 换行 + sha256(请求体))`
- 时间戳偏差超过 5 分钟拒绝；随机数只能用一次，防止重放
- 支持同时配置多把密钥，换密钥时不用停机

| 接口 | 机器人什么时候调用 | 返回 |
|---|---|---|
| `health` | 启动时、状态命令 | 配置是否正确、插件版本、服务器时间 |
| `groups` | 启动时、定期 | 受管群列表（群号、名称、类型）。机器人只管这张表里的群 |
| `check` | 巡检、新人入群、处理事件 | 对「群号 + 一组 QQ」逐个返回：`decision`、`reason`、应有的群名片 |
| `claim` | 每一个入群申请（有没有验证码都调用，由 AA 识别验证码） | 验证结果 + 判定 |
| `events` | 每分钟 | 上次之后的新事件 |

**判定只有三种（`decision`）：**

| decision | 含义 | 机器人应该怎么做 |
|---|---|---|
| `allow` | 有资格 | 批准申请 / 保留 |
| `deny` | 明确没有资格 | 按机器人那边的模式：只报告，或（开了踢人开关的群）处置 |
| `review` | 需要人工处理（例如冲突） | **永不处置**，只报告 |

**原因（`reason`）：** `OK`、`NOT_BOUND`（没绑定）、`PENDING_VERIFY`（待验证）、`CONFLICT`（冲突）、`USER_INACTIVE`（账号停用）、`NO_MAIN`（没有主角色）、`NO_ACCESS`（没有成员权限）、`GROUP_ROLE_MISSING`（不在小群要求的组里）。

接口返回的只有机器人做决定需要的东西，**不返回 AA 内部用户编号等信息**。

如果 AA 这边配置有误（例如没配密钥），接口返回 `503` 加 `misconfigured`，机器人把它当成「无法判断」，**绝不踢人**。

---

## 9. 对 AA 服务器的压力

| 来源 | 频率 | 说明 |
|---|---|---|
| 巡检 | 每群每 6 小时 1 次 | 一次请求查完整个群，查询次数固定，不随群人数增长 |
| 取事件 | 每分钟 1 次 | 一次极小的数据库查询 |
| 入群申请 | 有人申请时 | 每个申请 1 次 |
| 每日对账 | 每天 1 次 | 在 Celery 后台跑，不占网页进程 |

合计大约每分钟 1 次轻量请求。AA 不主动连接任何外部服务，也不增加推送任务。

---

## 10. IT 需要做的事（安装时，写进 README）

1. `pip install` 本插件
2. `local.py`：
   - `INSTALLED_APPS` 追加 `"qqbot"`
   - `APPS_WITH_PUBLIC_VIEWS` **追加** `"qqbot"`（不要整行覆盖别的插件）。这一步缺了，机器人的请求会被 AA 当成未登录而跳转到登录页
   - `QQBOT_API_KEYS = {...}`：机器人通信密钥，由所有者在自己电脑上生成后交给 IT
   - `CELERYBEAT_SCHEDULE` 追加每日对账任务
3. `python manage.py migrate`，然后重启 AA
4. `python manage.py check`：插件自带检查，上面任何一项漏配都会**直接报错**，不会等上线后才出问题

装好之后的事都在前台完成：在 admin 里把 `basic_access` 挂到 Member 状态、把 `qqbot.manage` 挂给管理员组，然后由 QQ 管理员在前台添加群。

---

## 11. 安全要点（来自旧版缺陷审计）

- 机器人接口放在**单独的路由分组**里，只对它豁免登录；其余页面全部显式要求登录和权限，不依赖 AA 的自动保护（AA 在这里有个会让其他页面丢失保护的坑）。
- 验证码只存哈希；消费是原子操作，同一个码并发使用只会成功一次。
- 所有密钥比较用常数时间。
- 接口限速；换绑、生成验证码有频率限制。
- 日志用 AA 的日志体系；INFO 级别不记录完整 QQ 号。
- 所有来自用户的文字（昵称、角色名）在 AA 页面里都做转义；机器人那边同样要转义（写进 `API.md` 提醒）。

---

## 12. 第一期范围

**做：**

- §2 两层权限；§3 群配置；§4 全部页面；§5 绑定流程（含冲突处理）；§6 群名片计算；§7 事件与每日对账；§8 全部接口 + `API.md`；§10 system check；§11 安全要点
- 自动化测试：能在云端安装 AA 5.2 跑测试，覆盖权限、判定、绑定流程、冲突、验证码、签名和重放、事件
- 给 IT 的安装说明

**以后再说：**

- AA 站内通知（例如「你的绑定失效了」）
- 按状态设置不同的群名片格式

**删除：** 现有的旧代码（自由填写的绑定表单、`api/bind` 接口、`ping`）全部删除并重写；数据库迁移从头开始（决定 #2）。

---

## 13. 还需要你之后提供的信息（不影响开工）

| # | 信息 | 什么时候需要 | 谁去查 |
|---|---|---|---|
| 1 | 正式 AA 的确切版本（`pip show allianceauth`） | 上线前 | IT |
| 2 | 正式 AA 的 State 配置（有哪些状态、谁是 Member） | 上线前 | IT 或有 AA 管理权限的人 |
| 3 | Celery beat 和 worker 是否正常运行 | 上线前 | IT |
| 4 | 测试环境：树莓派 AA 的访问方式 | 实测时 | 你 |
| 5 | QQ 群名片的实际长度上限 | Koishi 开发时实测 | 你 + 机器人 |
