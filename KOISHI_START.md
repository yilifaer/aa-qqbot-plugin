# Koishi 机器人插件 —— 开工说明

> 这份文件是给**新的 Koishi 插件项目**准备的开工说明，读者有两个：
>
> - **项目所有者**：照着「第 1 节」准备好新仓库和新会话。
> - **负责写 Koishi 插件的新 Claude Code 会话**：先读完这份文件，再读 [`API.md`](API.md)。
>
> 本仓库（aa-qqbot）只负责 AllianceAuth 这一侧。机器人那一侧（审批入群、巡检、踢人、改群名片）
> 放在一个**独立的仓库**里写（见 [`DECISIONS.md`](DECISIONS.md) 第 1 条）。
>
> 本文件不包含任何真实的群号、机器人 QQ、服务器地址或密钥。这些信息由所有者在新会话里当面提供。
> 密钥永远不要贴进对话。

---

## 目录

1. [开工前的准备（给所有者）](#1-开工前的准备给所有者)
2. [整体是怎么配合的](#2-整体是怎么配合的)
3. [必须遵守的红线](#3-必须遵守的红线)
4. [已经定好的事](#4-已经定好的事)
5. [还没定、开工时要问所有者的事](#5-还没定开工时要问所有者的事)
6. [测试环境](#6-测试环境)
7. [怎么接上树莓派上的测试 AA](#7-怎么接上树莓派上的测试-aa)
8. [建议的开发顺序](#8-建议的开发顺序)
9. [上线前必须通过的验证](#9-上线前必须通过的验证)
10. [参考资料](#10-参考资料)

---

## 1. 开工前的准备（给所有者）

### 1.1 新建 GitHub 仓库

- 仓库名建议沿用你已有插件的前缀，例如 **`koishi-plugin-igc-qqguard`**。
  注意：`koishi-plugin-qqmonitor-guard` 这个 npm 包名已经被别人占用了。
- 建议设成**私有**仓库。如果要公开，不要提交交接文档里的 `HANDOFF/` 文件夹（里面有群号、内网地址等内部信息）。
- 许可证建议 MIT，和 aa-qqbot 一致。

### 1.2 开一个新的 Claude Code 会话，并准备好这些资料

| 资料 | 从哪里来 | 作用 |
|---|---|---|
| 本仓库的 `KOISHI_START.md`（本文件） | aa-qqbot 仓库 | 开工说明 |
| 本仓库的 `API.md` | aa-qqbot 仓库 | 机器人和 AA 之间的**接口合同**，最重要 |
| 本仓库的 `DECISIONS.md` | aa-qqbot 仓库 | 已经和所有者确认过的决定 |
| 交接压缩包里的 `HANDOFF/` 文件夹 | 所有者电脑上 | 旧系统的缺陷审计、Koishi / OneBot 平台调研（带源码行号） |

最简单的做法：把 aa-qqbot 仓库也加进新会话，新会话就能直接读这三个文件；
再把 `HANDOFF/` 文件夹上传给新会话，或者放进新仓库（记得先把新仓库设为私有）。

### 1.3 给新会话的第一段话（可以直接复制）

```
我要写一个 Koishi 插件，配合我已经写好的 AllianceAuth 插件 aa-qqbot，管理联盟 QQ 群的成员。
请先读 aa-qqbot 仓库的 KOISHI_START.md、API.md、DECISIONS.md，再读 HANDOFF 文件夹
（README.md 必读，附录按需查阅）。
读完后先不要写代码：把 KOISHI_START.md 第 5 节里「还没定的事」逐条问我，
全部确认后写进新仓库的 DECISIONS.md，再按第 8 节的顺序开工。
我是电脑小白，请用中文一步步教我。
```

---

## 2. 整体是怎么配合的

```
   AllianceAuth（aa-qqbot 插件）                    Koishi（新插件）+ OneBot（LLBot）
   ──────────────────────────                    ───────────────────────────────
   成员在「服务」页绑定 QQ                          入群申请 ──► claim ──► 同意 / 留给人工
   QQ 管理员在前台配置群和成员         ◄── 签名 ──  巡检（每 6 小时）──► check（完整名单）
   判定：allow / deny / review            HTTPS   事件（每 60 秒）──► events ──► check
   群名片：AA 算好                                改群名片（只在 allow 且和现在不同时）
```

- **通信只有一个方向：机器人主动调用 AA。** AA 在公网，机器人在内网，AA 连不到机器人。
- AA 负责回答「这个 QQ 能不能进这个群、群名片应该叫什么」。机器人负责「怎么做、什么时候做、做多少」。
- **踢人开关只在机器人这边**（DECISIONS 第 11 条）。AA 前台再怎么改，也不会自动打开踢人。

---

## 3. 必须遵守的红线

这些来自交接文档 `HANDOFF/README.md` 第 0.2 节和旧系统的真实事故，没有例外：

1. **不在正式群里开发和测试。** 发消息、审批、踢人只能在第 6 节的**测试群**里做。
2. **不在正式机器人上加载开发版插件**，除非所有者针对那一次操作明确同意。
3. **正式机器人所在的电脑默认只读**：可以看日志和版本；重启、改配置、装卸插件都要所有者逐次同意。
4. **凭据不进对话、不进命令行、不进仓库。** 密钥由所有者在自己电脑上生成，自己填进 Koishi 控制台
   和 AA 的 `local.py`。读取配置文件时先把含 `secret` / `token` / `password` / `key` 的行过滤掉再输出。
5. **旧的 QQMonitor Koishi 插件保持禁用**，直到新插件上线。它的入群校验会作用到机器人所在的**每一个**群。
6. **Clean-room**：旧的两个仓库都没有 LICENSE。只按 `HANDOFF/01-旧系统功能规格.md` 描述的行为写，
   不打开、不复制旧源码或补丁版源码。
7. **「无法判断」绝不处置**：网络错误、超时、非 200、非 JSON、`review`、不认识的 `decision`，
   一律只报告，不踢人、不拒绝（API.md 第 1.1 节）。

---

## 4. 已经定好的事

来自 aa-qqbot 的 [`DECISIONS.md`](DECISIONS.md)，机器人这边直接照做：

| 事项 | 决定 | 来源 |
|---|---|---|
| 仓库 | 机器人插件放在独立仓库 | #1 |
| 受管的群 | 由 AA 前台配置，机器人通过 `groups` 接口获取；分固定群和身份组小群，**两种都由机器人管控** | #8、#9 |
| 踢人开关 | **只在机器人这边**，默认关闭（只报告） | #11 |
| 群名片 | 机器人按 AA 给的 `card` 自动同步，**第一期就做** | #12 |
| 新成员 | 入群申请的「验证信息」里填验证码。机器人**每一个**入群申请都调用 `claim`，原文照传 | #13、API.md 5.4 |
| 老成员 | 已经在群里的人在 AA 填 QQ 即可生效。前提是机器人巡检时上报了**完整名单**（`full_roster: true`） | #13、#14、API.md 第 7 节 |
| 冲突 | 两个账号认领同一个 QQ 时 AA 返回 `review`，**永不处置**，由 QQ 管理员在 AA 前台处理 | #14、#17 |
| 私聊验证 | **不做**（不需要机器人支持私聊收验证码） | #17 |
| 熔断 | 一轮要移出的人数超过阈值时，这个群这一轮一个都不移出，只报告并等管理员确认 | API.md 7.1 |

---

## 5. 还没定、开工时要问所有者的事

新会话**不要自己采用建议值**，逐条问所有者，确认后写进新仓库的 `DECISIONS.md`。

| # | 问题 | 选项 | 建议 |
|---|---|---|---|
| K1 | 插件名、npm 包名、许可证、发布方式 | npm 发布 / 只从 GitHub 或本地 `.tgz` 安装 | `koishi-plugin-igc-qqguard`，MIT。先用本地 `.tgz` 安装测试，稳定后再发 npm |
| K2 | 入群申请被判 `deny` 时 | 自动拒绝（附绑定指引）/ 留给管理员人工处理 | **先留给人工**，稳定后再考虑自动拒绝。`review` 和「无法判断」无论如何都留给人工 |
| K3 | 已在群里、被发现不合格的人（例如没经过申请就进了群） | 宽限期多长；怎么提醒（群里 @ / 私聊） | 宽限 24–72 小时，群里 @ 提醒（不是好友的私聊可能被 QQ 风控拦截，要实测） |
| K4 | 每个群的模式 | `off` / `report`（只报告）/ `enforce`（真处置） | 默认 `report`。某群第一次改成 `enforce` 前，至少要成功跑过一轮只报告，并由管理员确认 |
| K5 | 熔断和速率上限的数值 | — | 单轮超过「5 人或群人数的 10%」（取较小者）就整轮不处置；每群每小时最多移出 10 人 |
| K6 | 管理命令谁能用、在哪里用 | — | 同时满足：Koishi 权限 ≥ 3，**并且**在运维 QQ 名单里或是群管理员，**并且**只能在指定的管理群或私聊里用（交接文档 R13 要求「与」关系，不要照抄「满足任一」的 `permissions` 写法） |
| K7 | 通知发到哪里 | 运维群号 | 通知群只放运维人员，不能是受管群；每轮一条汇总，不逐人刷屏 |
| K8 | 成员自己在群里改了名片 | 机器人改回去 / 不管 | 建议下次巡检时改回 AA 给的名片（AA 名片才是准的） |
| K9 | 各受管群的加群方式 | — | 设成「需要发送验证信息」，否则入群申请拦截不到。「允许任何人加入」「邀请直接进群」的群只能靠巡检兜底 |
| K10 | 测试环境的具体信息 | 见第 6 节 | — |

---

## 6. 测试环境

没有这些，就不能做任何真实的审批、踢人测试：

| 需要 | 说明 |
|---|---|
| **测试机器人 QQ** | 一个单独的 QQ 号，不用正式机器人 |
| **测试群** | 测试机器人要是**管理员**（审批、踢人、改名片都需要） |
| **可以被踢的小号** | 至少 1–2 个，用来测绑定、验证码、踢人 |
| **Koishi 测试实例** | 放在哪台机器上（MacBook、树莓派，或正式 Mac mini 上**独立的第二个实例**），不和正式实例共用 |
| **测试 AA** | 树莓派上的测试 AA，已经装好 aa-qqbot（见第 7 节） |
| **开发版安装方式** | 本地 `.tgz` 或 `yarn link`。正式 Koishi 用的是 yarn 4，那里不要用 npm |

正式环境的版本（写代码时以这些为准，详见 `HANDOFF/03-平台调研.md`）：
Koishi Desktop 4.18.11、`koishi-plugin-adapter-onebot` 6.9.4、`@koishijs/plugin-http` 0.6.3、
OneBot 实现为 LLBot，已启用 `database-sqlite`。

---

## 7. 怎么接上树莓派上的测试 AA

1. **树莓派上装好 aa-qqbot**：按 aa-qqbot 仓库的 [`README.md`](README.md) 操作，包括 `APPS_WITH_PUBLIC_VIEWS`
   和 `QQBOT_API_KEYS` 两项配置，并按 README 第 2 节第 7 步确认装好了。Koishi 插件写好之前，可以先用那里的
   「模拟机器人」和 [`docs/TESTING.md`](docs/TESTING.md) 把 AA 这一侧测一遍。
2. **生成一把测试密钥**（所有者在自己电脑上运行，不要贴进对话）：
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(48))"
   ```
   把它写进树莓派的 `local.py`，例如 `QQBOT_API_KEYS = {"koishi-test": "刚才生成的那串"}`，然后重启 AA。
   同一把密钥由所有者自己填进 Koishi 控制台的插件配置里。
   如果装 aa-qqbot 时已经按 README 第 2 节第 2 步生成过密钥，直接用那一把和它的密钥编号即可，不用再生成。
3. **机器人要能访问到树莓派**：最简单是直接用树莓派 AA 的 `SITE_URL`（AA 的网址）加 `/qqbot/api/v1/`。
   测试时也可以用局域网地址，例如 `http://<树莓派的IP>/qqbot/api/v1/`，但要先让 IT 在树莓派 AA 的 `local.py` 里
   追加 `ALLOWED_HOSTS += ["<树莓派的IP>"]` 并重启 AA，否则会得到 400 网页（API.md 第 6 节）。
   正式环境必须用 HTTPS。
4. **先测连通**：插件的第一个功能就做「调用 `health` 并显示结果」。返回 `ok: true` 说明签名和网络都通了。
   - 返回 **302** 或一个登录网页：AA 那边 `APPS_WITH_PUBLIC_VIEWS` 没配好（API.md 第 6 节）。
   - 返回 **400 网页**：AA 的 `ALLOWED_HOSTS` 不包含你用的地址（见上一条）。
   - 返回 **401 `bad_signature`**：两边签名算法对不上。先让插件的单元测试通过 API.md 第 2.5 节的**签名测试样例**。
   - 返回 **401 `stale_timestamp`**：两台机器的时间差太多，打开自动对时（NTP）。
5. **在 AA 前台建测试群**：用 QQ 管理员账号打开侧边栏「QQ 管理」→「QQ 群」，添加**测试群**的群号。
   机器人只管理 `groups` 接口返回的群。

---

## 8. 建议的开发顺序

每一步都要有自动化测试。先做「只看不动」的部分，处置（踢人）放在最后。

| 阶段 | 内容 | 完成标准 |
|---|---|---|
| **M0 骨架** | 配置项（带上下界校验）、`inject: { required: ['database', 'http'] }`、签名模块、HTTP 客户端（`redirect: 'manual'`、只收 200、超时、有限重试） | 签名单元测试通过 API.md 2.5 的样例；`health` 命令在测试群能显示结果 |
| **M1 只报告的巡检** | `groups` → 取群成员 → `check`（`full_roster: true`）→ 每群一条汇总发到运维群 | 在测试群跑通；AA 管理页「待处理」里能看到「群里有但没绑定」的 QQ |
| **M2 入群申请** | 每个申请都调用 `claim`；`allow` 同意；其余按 K2 处理 | 小号带验证码申请 → 自动通过，AA 卡片显示「已验证」 |
| **M3 事件** | 每 60 秒拉 `events`，游标存数据库；一页事件按群合并成一次 `check` | 在 AA 前台解绑小号后，一两分钟内机器人报告该 QQ 变成 `deny` |
| **M4 群名片** | `allow` 且和当前名片不同时才改；文字按纯文本传 | 在 AA 改昵称后，测试群名片自动更新 |
| **M5 真处置** | 按群模式 `enforce`；处置前实时复核（`getGroupMemberInfo`）；群主、管理员、机器人自己、白名单永不处置；熔断和速率上限；`pause` / `resume` / `status` / `patrol` 命令 | 只在测试群、对小号完成一次「巡检 → 踢出 → 汇总通知」 |
| **M6 验收** | 跑完第 9 节全部验证 | 所有者确认后，按交接文档 §8.2 的步骤上线 |

---

## 9. 上线前必须通过的验证

交接文档 `HANDOFF/README.md` §9.5 的第 9–20 条是 Koishi 端必须通过的项目。最关键的几条：

- AA 不可达、返回 302、返回网页、返回 5xx、超时 → **全部不处置**，也不重试 3xx/4xx。
- AA 对全员返回合法的 `deny`（模拟 AA 误配置）→ 触发熔断，整轮不处置，需要管理员确认。
- 群主、管理员、机器人自己、白名单**永远不会被处置**。
- 不在受管群列表里的群，任何事件都忽略。
- 巡检不会重叠；`pause`、停用插件、改配置都能立即中止正在进行的一轮。
- 通知发送失败不影响处置结果和记录。
- 角色名、昵称、错误消息里带 `<at type="all"/>` 等内容时，按纯文本输出。
- 管理命令鉴权：群管理员但权限不足 → 拒绝；在受管群里用管理命令 → 拒绝。
- **真实踢人**只在测试群、只对小号做。

另外，API.md 第 11 节有一份逐条的「Koishi 端实现清单」，写完代码后逐条打勾。

---

## 10. 参考资料

| 资料 | 内容 |
|---|---|
| [`API.md`](API.md) | 接口契约：签名、每个接口的字段、错误码、熔断、事件轮询、群名片、建议频率、实现清单 |
| [`DECISIONS.md`](DECISIONS.md) | 已确认的决定 |
| [`DESIGN.md`](DESIGN.md) | AA 这一侧的设计（成员和管理员看到什么） |
| `HANDOFF/README.md` | 交接总览：根因、需求（R/S/N 条目）、红线、测试矩阵、踩坑清单 |
| `HANDOFF/02-旧系统缺陷清单.md` | 旧 Koishi 插件的 56 条缺陷（D1–D56），写代码和评审时逐条对照 |
| `HANDOFF/03-平台调研.md` | Koishi 4.18.11、adapter-onebot 6.9.4、plugin-http 0.6.3、LLBot 的 API 事实和坑（带源码行号） |
