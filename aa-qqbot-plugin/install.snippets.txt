# ===== Add to INSTALLED_APPS (local.py) =====
INSTALLED_APPS += [
    "qqbot",
]

# ===== Add to URLConf (project urls.py) =====
from django.urls import include, path
urlpatterns += [
    path("qqbot/", include("qqbot.urls")),
]
